import PayForm from './PayForm';
import Link from 'next/link';
const layoutStyle = {
  margin: 20,
  padding: 20,
  border: '1px solid #DDD'
};

const Layout = () => (
  <Link href="/">
    <button>home</button>
  </Link>
);

export default Layout;
